name: Workflow02
on: 
  push:
    branches:
    - 'main'
  workflow_dispatch:  
jobs:
  job01:
    env:
      message: 'Hello Everyone!'
    runs_on: ubuntu-latest
    steps:
    - name: Display Message
      run: |
        echo ${{ env.message }}